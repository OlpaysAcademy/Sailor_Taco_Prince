import React from 'react';

const selectStyle = {
    marginTop: 100
}

const SelectRecipeMsg = () => {
    return (
        <div style={selectStyle} className="row text-center">
            <h2>
                Please select a recipe
            </h2>
        </div>
    )
}

export default SelectRecipeMsg;
